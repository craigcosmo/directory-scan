{
  "name": "webpack-directory-scan",
  "version": "1.1.31",
  "description": "return an array which contains all folders and subfolers of a provided directory ",
  "main": "./dist/index.js",
  "scripts": {
    "test": "mocha --watch --compilers js:babel-register",
    "patch": "npm version patch",
    "publish": "npm version patch && npm run compile && npm publish ",
    "compile": "babel --presets es2015,stage-0 -d dist/ index.js"
  },
  "author": "craig nguyen",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.16.0",
    "babel-core": "^6.18.2",
    "babel-preset-es2015": "^6.18.0",
    "babel-preset-stage-0": "^6.16.0",
    "chai": "^3.5.0",
    "mocha": "^3.1.2"
  },
  "dependencies": {
    "glob": "^7.1.1",
    "recursive-readdir-sync": "^1.0.6"
  },
  "babel": {
    "presets": [
      "es2015"
    ]
  }
}
